<h2>À écouter plus tard</h2>
<table>
  <tr>
    <th>Album</th>
    <th>Artiste</th>
    <th></th>
    <th></th>
  </tr>
  <% @liste.each do |element| %>
      <tr>
        <td><%= element.album.name %></td>
        <td><%= element.album.artiste.nom %></td>
        <td><%= link_to "C'est fait", changerListe_path(:id => element.id), :method => :post %></td>
        <td></td>
      </tr>
  <%end%>

</table>
