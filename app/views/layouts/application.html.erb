<!DOCTYPE html>
<html>
    <head>
      <title>CDCollection</title>
      <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
      <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
      <%= csrf_meta_tags %>
      <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
      <![endif]-->
    </head>
    <body>
        <!--HEADER-->
        <header>
            <div class="page-header">
                <div class="container">
                    <% if current_user %>
                        <div>
                            <h1 class="pull-left"><%= link_to "CDCollection", profil_show_path(:id => current_user.id)%></h1>
                        </div>
                    <% else %>
                          <div>
                            <h1 class="pull-left"><%= link_to "CDCollection", root_path%></h1>
                          </div>
                    <% end %>
                    <p class="pull-right">
                        <% if current_user %>
                            <%= link_to "Se déconnecter", signed_out_path, :class =>"btn btn-primary btn-xs"  %>
                        <% end %>
                    </p>
                </div>
            </div>
            <% if current_user %>
                <div class="navbar-inner">
                    <div class="container">
                        <div class="text-right">
                            <%= form_tag(artiste_index_path, method: "get")  do %>
                                <%= text_field_tag :search, params[:search] %>
                                <%= submit_tag "Rechercher", :name => nil, :class => "btn btn-danger" %>
                            <% end %>
                        </div>
                        <nav>
                            <ul class="list-inline text-center">
                                <li><%= link_to "Albums notés",profil_path(:id => current_user.id) %></li>|
                                <li><%= link_to "Ma collection", collection_path %></li>|
                                <li><%= link_to "À écouter plus tard",plus_tard_path %></li>
                            </ul>
                        </nav>
                    </div>
                </div>
            <% end %>
        </header>

        <% flash.each do |name, msg| %>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>

        <div class="container">
            <%= yield %>
        </div>
    </body>
</html>
